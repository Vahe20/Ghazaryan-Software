import style from "../style.module.scss";

export const Header = () => {
    return (
        <div className={style.header}>
            <h1>Welcome Back</h1>
            <p>Login to your account</p>
        </div>
    );
};
